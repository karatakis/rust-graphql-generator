# Database to GraphQL API generator

**WARNING: WORK IN PROGRESS**

The purpose of this tool is to generate a GraphQL compatible API for an SQL database.

## Example

```bash
$ cargo run generated
$ cd ./generated
$ cargo run
```

